# iPhone notes

Grok iPhone preview is WKWebView.

- Do not request GPS on load (blocks the shop behind a location prompt).
- Add piece / Add crate are `<form>` + `type="submit"` so iOS always fires a tap.
- Sticky header is logo-only so product photos are not covered.
- Open/closed is a staff button, not a clock.
